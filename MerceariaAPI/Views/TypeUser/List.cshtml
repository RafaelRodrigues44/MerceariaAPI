@model IEnumerable<MerceariaAPI.Areas.Identity.Models.TypeUser>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Lista de Tipos de Usuários</h2>

<table>
    <thead>
        <tr>
            <th>Id</th>
            <th>Nome</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Nome</td>
                 <td>
                    <a href="/TypeUser/Edit/@item.Id">Editar</a> |
                    <a href="/TypeUser/Delete/@item.Id">Excluir</a> |                   
                </td> 
            </tr>
        }
    </tbody>
</table>

<a href="/TypeUser/Create">Adicionar</a>
